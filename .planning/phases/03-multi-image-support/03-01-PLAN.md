---
phase: 03-multi-image-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ImagePreviewStrip.jsx
  - src/components/AstraApp.jsx
autonomous: true

must_haves:
  truths:
    - "User sees image count indicator when images are attached"
    - "Indicator shows current count and maximum (e.g., '3/5')"
    - "Indicator has visual distinction when at limit (5/5)"
    - "Indicator is accessible to screen readers"
  artifacts:
    - path: "src/components/ImagePreviewStrip.jsx"
      provides: "Image count indicator component"
      contains: "ImageCountIndicator"
    - path: "src/components/AstraApp.jsx"
      provides: "maxImages prop passed to ImagePreviewStrip"
      pattern: "maxImages={MAX_IMAGES}"
  key_links:
    - from: "src/components/AstraApp.jsx"
      to: "src/components/ImagePreviewStrip.jsx"
      via: "maxImages prop"
      pattern: "maxImages="
---

<objective>
Add image count indicator to ImagePreviewStrip showing "X/5" when images are attached.

Purpose: Fulfill MULT-02 requirement - user needs clear feedback on how many images are attached and the maximum allowed. MULT-01 (multi-image support) and MULT-03 (individual removal) are already complete from Phase 1.

Output: ImagePreviewStrip displays count indicator after thumbnails with visual distinction at limit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-image-support/03-RESEARCH.md
@src/components/ImagePreviewStrip.jsx
@src/components/AstraApp.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ImageCountIndicator to ImagePreviewStrip</name>
  <files>src/components/ImagePreviewStrip.jsx</files>
  <action>
Add an ImageCountIndicator component inside ImagePreviewStrip.jsx that displays the current image count vs maximum.

**Create ImageCountIndicator component:**
- Accept props: `count`, `max`, `theme`
- Render a pill-shaped badge showing `{count}/{max}`
- Normal state (count < max): `backgroundColor: ${theme.textSecondary}10`, `color: theme.textSecondary`
- At-limit state (count === max): `backgroundColor: ${theme.accentSoftBlue}20`, `color: theme.accentSoftBlue`
- Add `aria-label={count} of {max} images attached` for accessibility
- Style: `fontSize: 12`, `fontWeight: 500`, `padding: 4px 10px`, `borderRadius: 12`, `whiteSpace: nowrap`, `flexShrink: 0`

**Update ImagePreviewStrip component:**
- Add `maxImages` prop to component signature
- Render ImageCountIndicator after the images.map() loop, inside the flex container
- Pass `count={images.length}`, `max={maxImages}`, `theme={theme}` to indicator

**Pattern reference (from BillingModal.jsx Badge):**
```javascript
display: 'inline-flex',
alignItems: 'center',
borderRadius: 999,
padding: '4px 12px',
fontSize: 12,
fontWeight: 600
```

The indicator should be the last item in the flex container, after all thumbnails.
  </action>
  <verify>
1. `grep -n "ImageCountIndicator" src/components/ImagePreviewStrip.jsx` shows component definition and usage
2. `grep -n "maxImages" src/components/ImagePreviewStrip.jsx` shows prop in signature
3. `grep -n "aria-label" src/components/ImagePreviewStrip.jsx` shows accessibility attribute
  </verify>
  <done>
- ImageCountIndicator component exists in ImagePreviewStrip.jsx
- ImagePreviewStrip accepts maxImages prop
- Indicator renders after thumbnails with count/max format
- Indicator has at-limit styling when count === max
- Indicator has aria-label for screen readers
  </done>
</task>

<task type="auto">
  <name>Task 2: Pass maxImages prop from AstraApp</name>
  <files>src/components/AstraApp.jsx</files>
  <action>
Update the ImagePreviewStrip usage in AstraApp.jsx to pass the maxImages prop.

**Find the ImagePreviewStrip render (around line 3565):**
```jsx
<ImagePreviewStrip
  images={selectedImages}
  onRemove={onRemoveImage}
  theme={theme}
  isMobile={isMobile}
/>
```

**Add maxImages prop:**
```jsx
<ImagePreviewStrip
  images={selectedImages}
  onRemove={onRemoveImage}
  theme={theme}
  isMobile={isMobile}
  maxImages={MAX_IMAGES}
/>
```

Note: MAX_IMAGES is already imported from ImageInputManager.jsx (line 38).
  </action>
  <verify>
1. `grep -n "maxImages={MAX_IMAGES}" src/components/AstraApp.jsx` shows prop passed
2. Run `npm run dev` and attach 1-5 images - verify counter appears and updates
3. Verify at 5/5 the indicator shows accent color styling
  </verify>
  <done>
- maxImages={MAX_IMAGES} prop passed to ImagePreviewStrip in AstraApp.jsx
- Counter displays correctly when images attached
- Counter updates when adding/removing images
- Counter shows visual distinction at 5/5 limit
  </done>
</task>

</tasks>

<verification>
**Functional verification:**
1. Start dev server: `npm run dev`
2. Attach 1 image - verify "1/5" indicator appears after thumbnail
3. Attach 2 more images - verify "3/5" indicator updates
4. Attach 2 more to reach limit - verify "5/5" shows with accent color
5. Remove 1 image - verify "4/5" returns to normal styling
6. Remove all images - verify indicator disappears (strip returns null)

**Accessibility verification:**
- Use browser dev tools to inspect aria-label on indicator element

**Mobile verification:**
- Resize browser to mobile width, verify indicator doesn't cause layout issues
</verification>

<success_criteria>
- [ ] Image count indicator visible when 1+ images attached
- [ ] Indicator shows "X/5" format (current/max)
- [ ] Indicator has accent color styling at limit (5/5)
- [ ] Indicator has aria-label for screen readers
- [ ] Counter updates correctly when adding/removing images
- [ ] No TypeScript/lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-image-support/03-01-SUMMARY.md`
</output>
