---
phase: 05-pdf-support
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/AstraApp.jsx
  - src/components/ImageInputManager.jsx
autonomous: true

must_haves:
  truths:
    - "User can select PDF files in the file picker"
    - "PDF pages are converted to images and appear in preview strip"
    - "User sees loading indicator during PDF processing"
    - "Error message shown for encrypted or corrupted PDFs"
  artifacts:
    - path: "src/components/AstraApp.jsx"
      provides: "PDF handling in file input and loading state"
      contains: "application/pdf"
    - path: "src/components/ImageInputManager.jsx"
      provides: "PDF validation acceptance"
      contains: "application/pdf"
  key_links:
    - from: "src/components/AstraApp.jsx"
      to: "src/utils/pdfUtils.js"
      via: "import and function call"
      pattern: "import.*processPdfToImages.*pdfUtils"
    - from: "src/components/AstraApp.jsx"
      to: "ImageInputManager.addImages"
      via: "passing converted images"
      pattern: "addImages.*imageFiles"
---

<objective>
Integrate PDF processing into file input and add loading indicator during PDF conversion.

Purpose: Complete PDF upload feature by connecting utility to UI with proper feedback
Output: Users can upload PDFs and see them converted to images with loading feedback
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pdf-support/05-RESEARCH.md
@.planning/phases/05-pdf-support/05-01-SUMMARY.md

# Files to modify
@src/components/AstraApp.jsx
@src/components/ImageInputManager.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update file input to accept PDFs</name>
  <files>src/components/AstraApp.jsx, src/components/ImageInputManager.jsx</files>
  <action>
1. In AstraApp.jsx, find the file input element (around line 3700-3710):
   - Change accept attribute from `"image/jpeg,image/png,image/gif,image/webp"`
   - To `"image/jpeg,image/png,image/gif,image/webp,application/pdf"`

2. In ImageInputManager.jsx, update ALLOWED_IMAGE_TYPES constant (line ~5):
   - Add 'application/pdf' to the array
   - Or create separate ALLOWED_PDF_TYPE constant for clarity

3. Update validateImageFile function to accept PDFs:
   - Either add 'application/pdf' to ALLOWED_IMAGE_TYPES
   - Or add separate check: `file.type === 'application/pdf'`
   - Note: PDFs will be processed differently, so we just need to allow them through validation

Rationale: PDF detection will happen in handleFileSelect, which will branch to PDF-specific processing. The validation just needs to not reject PDFs.
  </action>
  <verify>
- `grep "application/pdf" src/components/AstraApp.jsx` shows accept attribute updated
- `grep "application/pdf" src/components/ImageInputManager.jsx` shows type allowed
  </verify>
  <done>File input accepts PDF files, validation allows PDF type through</done>
</task>

<task type="auto">
  <name>Task 2: Add PDF processing state and loading indicator</name>
  <files>src/components/AstraApp.jsx</files>
  <action>
1. Add state variables in AstraApp component (near other useState calls):
   ```javascript
   const [isPdfProcessing, setIsPdfProcessing] = useState(false);
   const [pdfProgress, setPdfProgress] = useState(null); // { current: 1, total: 5 }
   ```

2. Import processPdfToImages utility:
   ```javascript
   import { processPdfToImages } from '../utils/pdfUtils';
   ```

3. Modify handleFileSelect in InputBar component to handle PDFs:
   - Receive isPdfProcessing, setIsPdfProcessing, setPdfProgress as props
   - Detect PDF: `file.type === 'application/pdf'`
   - For PDFs:
     - Set isPdfProcessing = true
     - Calculate available slots: `MAX_IMAGES - selectedImages.length`
     - Call processPdfToImages with progress callback
     - Pass converted image files to onAddImages
     - Handle errors (catch block shows error via onSetError)
     - Set isPdfProcessing = false in finally block
   - For images: existing flow (just call onAddImages)

4. Add loading indicator UI in InputBar:
   - Show above the input area when isPdfProcessing is true
   - Display: "Processing PDF: page X of Y"
   - Use existing app styling (text color from theme context)
   - Include a simple spinner or pulsing dot animation

5. Pass new props to InputBar component where it's rendered

Key error messages to show:
- "PDF is password protected" (PasswordException from PDF.js)
- "Could not read PDF file" (general parsing errors)
- "PDF has no pages" (numPages === 0)
- Generic: "Failed to process PDF: {error.message}"

Progress callback pattern:
```javascript
const onProgress = (current, total) => setPdfProgress({ current, total });
```

After processing completes (success or error), clear progress:
```javascript
setPdfProgress(null);
```
  </action>
  <verify>
- `grep "isPdfProcessing" src/components/AstraApp.jsx` shows state
- `grep "processPdfToImages" src/components/AstraApp.jsx` shows import and usage
- `grep "Processing PDF" src/components/AstraApp.jsx` shows loading indicator text
  </verify>
  <done>PDF processing integrated with loading indicator showing page progress</done>
</task>

<task type="auto">
  <name>Task 3: Handle edge cases and multi-page truncation</name>
  <files>src/components/AstraApp.jsx</files>
  <action>
1. Before calling processPdfToImages, check if already at image limit:
   ```javascript
   const availableSlots = MAX_IMAGES - selectedImages.length;
   if (availableSlots <= 0) {
     onSetError(`Maximum ${MAX_IMAGES} images already attached`);
     return;
   }
   ```

2. Pass availableSlots as maxPages to processPdfToImages:
   ```javascript
   const imageFiles = await processPdfToImages(file, availableSlots, onProgress);
   ```

3. After successful processing, if PDF had more pages than processed, show info message:
   - Track if truncation occurred (compare pdfDoc.numPages vs processed)
   - Show: "Processed X of Y pages (maximum 5 images)" as info, not error
   - Could use a toast/notification or brief console.log (keep simple for v1)
   - For v1, the existing image count indicator (3/5) provides sufficient feedback

4. Handle drag-drop PDFs:
   - In handleDrop function, update the filter to include PDFs:
   - Change: `file.type.startsWith('image/')`
   - To: `file.type.startsWith('image/') || file.type === 'application/pdf'`

5. Disable file input during PDF processing:
   - Pass isPdfProcessing to InputBar
   - Disable upload button when isPdfProcessing is true
   - Prevent accepting more files while processing

Edge case handling in processPdfToImages call:
- Empty PDF (0 pages): utility returns empty array, existing addImages handles gracefully
- Encrypted PDF: PDF.js throws PasswordException, caught in try/catch, show error
- Corrupted PDF: PDF.js throws generic error, caught in try/catch, show error
  </action>
  <verify>
- `grep "availableSlots" src/components/AstraApp.jsx` shows limit checking
- `grep "isPdfProcessing" src/components/AstraApp.jsx` shows it's passed to InputBar
- Drag-drop includes PDF: `grep "application/pdf" src/components/AstraApp.jsx`
  </verify>
  <done>Edge cases handled: limit checking, multi-page truncation, drag-drop PDFs, disabled during processing</done>
</task>

</tasks>

<verification>
1. Manual testing flow:
   - Start dev server: `npm run dev`
   - Click upload button - file picker should show PDF option
   - Select a PDF file - should see "Processing PDF: page X of Y"
   - After processing - PDF pages appear as image thumbnails
   - Count indicator updates correctly

2. Edge case verification:
   - Select 3 images, then a 5-page PDF - should only process 2 pages
   - Select encrypted PDF - should show error message
   - Drag and drop PDF onto input area - should process

3. Code verification:
   - `grep -c "application/pdf" src/components/AstraApp.jsx` returns 2+ (accept attr + drag check)
   - `grep "processPdfToImages" src/components/AstraApp.jsx` shows import and call
   - `grep "isPdfProcessing" src/components/AstraApp.jsx` shows state management
</verification>

<success_criteria>
- File picker shows PDF option (application/pdf in accept)
- PDF files trigger processPdfToImages flow
- Loading indicator shows "Processing PDF: page X of Y"
- Converted pages appear in ImagePreviewStrip as thumbnails
- Image count indicator reflects PDF pages (e.g., "3/5 images")
- Error message shown for encrypted/corrupted PDFs
- Upload button disabled during PDF processing
- Drag-drop accepts PDF files
- Respects MAX_IMAGES limit (processes only available slots)
</success_criteria>

<output>
After completion, create `.planning/phases/05-pdf-support/05-02-SUMMARY.md`
</output>
