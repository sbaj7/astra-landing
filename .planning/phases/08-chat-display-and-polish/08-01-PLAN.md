---
phase: 08-chat-display-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ImageLightbox.jsx
  - src/components/AstraApp.jsx
autonomous: true

must_haves:
  truths:
    - "User can click/tap on a chat image thumbnail to view full size"
    - "Full-size image displays in centered overlay with dark backdrop"
    - "User can close lightbox by clicking backdrop, X button, or pressing Escape"
    - "Lightbox only works for messages with actual image data (current session)"
  artifacts:
    - path: "src/components/ImageLightbox.jsx"
      provides: "Lightbox overlay component"
      exports: ["default"]
    - path: "src/components/AstraApp.jsx"
      provides: "Lightbox state and click handlers"
      contains: "lightboxImage"
  key_links:
    - from: "src/components/AstraApp.jsx"
      to: "src/components/ImageLightbox.jsx"
      via: "import and render"
      pattern: "import ImageLightbox"
    - from: "message image onClick"
      to: "setLightboxImage"
      via: "click handler"
      pattern: "onClick.*setLightboxImage"
---

<objective>
Create a full-size image lightbox overlay that opens when users click on image thumbnails in chat messages.

Purpose: Enables users to view uploaded images at full size for better examination (DISP-03).
Output: ImageLightbox component integrated into AstraApp with click-to-expand functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-chat-display-and-polish/08-RESEARCH.md

Key patterns from research:
- Modal overlay pattern: CitationPillOverlay.jsx (z-index: 50)
- Higher z-index needed: PaywallModal uses 1000, BillingModal uses 1200
- Use z-index: 1100 for lightbox (above PaywallModal, below BillingModal)
- Click backdrop to close, stopPropagation on image
- Escape key listener with useEffect cleanup
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImageLightbox component</name>
  <files>src/components/ImageLightbox.jsx</files>
  <action>
Create a new ImageLightbox component that displays a full-size image in a modal overlay.

Component signature:
```jsx
const ImageLightbox = ({ imageUrl, onClose, theme }) => { ... }
```

Requirements:
- Fixed position overlay covering viewport (position: fixed, inset: 0)
- z-index: 1100 (above PaywallModal at 1000, below BillingModal at 1200)
- Dark semi-transparent backdrop (backgroundColor: rgba(0, 0, 0, 0.9))
- Centered image with max-width: 90vw and max-height: 90vh
- objectFit: contain to preserve aspect ratio
- borderRadius: 8px on image
- cursor: zoom-out on backdrop (visual hint it's closable)
- Close button (X) positioned top-right with theme.textPrimary color
- onClick on backdrop calls onClose
- stopPropagation on image click (prevent closing when clicking image)
- useEffect for Escape key listener that calls onClose
- Return null if no imageUrl provided
- Use inline styles following CitationPillOverlay pattern (no Tailwind)
- Use X icon from lucide-react for close button
  </action>
  <verify>
File exists at src/components/ImageLightbox.jsx with exported component.
No linting errors: `npm run lint -- --quiet`
  </verify>
  <done>
ImageLightbox component created with dark overlay, centered image, X close button, backdrop click close, and Escape key support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate lightbox into AstraApp</name>
  <files>src/components/AstraApp.jsx</files>
  <action>
Integrate ImageLightbox into AstraApp with state management and click handlers.

1. Add import at top of file:
   ```jsx
   import ImageLightbox from './ImageLightbox';
   ```

2. Add lightbox state near other modal states (around line 4554 where showPaywall is defined):
   ```jsx
   const [lightboxImage, setLightboxImage] = useState(null);
   ```

3. Find the user message image rendering (around line 2807 where message.images.map is):
   Update the img element to add onClick handler and cursor: pointer style:
   ```jsx
   {message.images.map((img, idx) => (
     <img
       key={img.id || idx}
       src={img.data}
       alt={`Attached image ${idx + 1}`}
       style={{
         maxWidth: '120px',
         maxHeight: '120px',
         borderRadius: '8px',
         objectFit: 'cover',
         cursor: 'pointer'  // ADD THIS
       }}
       onClick={() => setLightboxImage(img.data)}  // ADD THIS
     />
   ))}
   ```

4. Render ImageLightbox at the end of the component (after PaywallModal, before final closing div):
   ```jsx
   <ImageLightbox
     imageUrl={lightboxImage}
     onClose={() => setLightboxImage(null)}
     theme={theme}
   />
   ```

Important: Only message.images (current session) gets click handler. The hadImages placeholder (reloaded sessions) does NOT get click handler since there's no image data.
  </action>
  <verify>
App compiles without errors: `npm run dev` (check terminal for errors)
Click an image thumbnail in chat - lightbox opens with full-size image
Click backdrop or X or press Escape - lightbox closes
  </verify>
  <done>
User can click image thumbnails in user messages to open full-size lightbox, and close via backdrop/X/Escape.
  </done>
</task>

</tasks>

<verification>
1. `npm run lint -- --quiet` passes without errors
2. `npm run build` succeeds
3. Manual test: Upload an image, send message, click thumbnail - lightbox appears
4. Manual test: Close lightbox via X button, backdrop click, and Escape key
5. Verify lightbox appears above other content (z-index working)
</verification>

<success_criteria>
- DISP-03 complete: User can tap/click image to view in full-size lightbox
- ImageLightbox component exists with proper styling
- Lightbox state managed in AstraApp
- Three close methods work (X, backdrop, Escape)
- Only current-session images are clickable (not reloaded placeholders)
</success_criteria>

<output>
After completion, create `.planning/phases/08-chat-display-and-polish/08-01-SUMMARY.md`
</output>
